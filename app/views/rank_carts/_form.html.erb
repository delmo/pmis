<%= form_for(@rank_cart) do |f| %>
  <% if @rank_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rank_cart.errors.count, "error") %> prohibited this rank_cart from being saved:</h2>

      <ul>
      <% @rank_cart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
