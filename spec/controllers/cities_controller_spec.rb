describe 'GET #show' do
=begin
 it "assigns the requested city to @city" do
  city = create(:city)
  get :show, id: city
  expect(assigns(:city)).to eq contact
 end

 it "renders the :show template" do
  city = create(:city)
  get :show, id: city
  expect(response).to render_template :show
 end
=end
end
